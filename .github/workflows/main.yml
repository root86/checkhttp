name: CI
on:
  push:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - shell: bash
      - run: |
          url=http://120.25.164.233:8080/appstore/app/checkversion?id=21
          content=$(curl -L $url)
          echo $content
